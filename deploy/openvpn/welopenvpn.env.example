# Existing platform API. This must be reachable from the Ubuntu OpenVPN host.
WEL_API_BASE_URL=http://127.0.0.1:8082/api/v1

# Shared secret used by OpenVPN server-side scripts to sync assigned VPN IPs
# back to the platform API.
OPENVPN_INTERNAL_SECRET=replace-with-a-long-random-secret
